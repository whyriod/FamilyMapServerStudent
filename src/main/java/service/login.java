package service;

import request.loginRequest;
import result.loginResult;

public class login {

    /***
     * Description: Logs in the user and returns an auth token.
     * @param r loginRequest
     * @return loginResult object
     */
    private loginResult login(loginRequest r){
        return null;
    }
}
